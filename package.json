{
  "name": "animehunt-backend",
  "version": "2.0.0-node",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev":   "node --watch src/index.js",

    "_comment_legacy": "Kept for the one-time migration step only (exporting your existing D1 data before cutover). Not used day-to-day once you're running on Node — remove the wrangler devDependency below after you've exported.",
    "db:export-from-d1": "wrangler d1 export animehunt --output=schema-and-data.sql"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@hono/node-server": "^1.13.0",
    "@libsql/client": "^0.14.0",
    "ioredis": "^5.4.1",
    "@aws-sdk/client-s3": "^3.658.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
